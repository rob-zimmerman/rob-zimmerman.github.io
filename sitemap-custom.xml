---
layout: null
permalink: /sitemap-custom.xml
---
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">

  {% assign base = site.url | append: site.baseurl %}

  {% for page in site.pages %}
    {% if page.url and page.sitemap != false and page.url != "/sitemap.xml" and page.url != "/sitemap-custom.xml" and page.url != "/feed.xml" %}
  <url><loc>{{ base }}{{ page.url | replace:'index.html','' }}</loc></url>
    {% endif %}
  {% endfor %}

  {% for post in site.posts %}
  <url><loc>{{ base }}{{ post.url }}</loc></url>
  {% endfor %}

  {% for collection in site.collections %}
    {% if collection.output %}
      {% for doc in collection.docs %}
        {% if doc.url and doc.sitemap != false %}
  <url><loc>{{ base }}{{ doc.url }}</loc></url>
        {% endif %}
      {% endfor %}
    {% endif %}
  {% endfor %}

</urlset>
